name: ci

on:
  push:
    branches: [main]
  pull_request:

jobs:
  unix:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v7
      # The committed kernels_metal.h / kernels_ptx.h are what every build
      # embeds, so an edit to kernels.metal can be reviewed and merged while
      # the shipped binary still carries the OLD shader bytes. `make test` runs
      # this gate too (see the make-test job), but it is also named here so it
      # runs on macOS and ahead of the build: pure Python, no Metal or CUDA
      # toolchain, well under a second, and it needs nothing the build produces.
      - name: generated GPU header drift gate
        run: python3 scripts/check-generated.py
      - name: build
        run: make
      - name: reject invalid CLI values
        run: |
          for args in '--port 70000' '--gpu typo' '--kv typo' '--reserve 101'; do
            if ./runner --caps $args > /dev/null 2>&1; then
              echo "unexpectedly accepted $args"; exit 1
            fi
          done
      # The loopback-only bind is a product decision, so it gets a gate rather
      # than a comment: no INADDR_ANY, no --host/--bind/--listen, no env var.
      # The runtime half (server genuinely unreachable off-box) is in the
      # conformance job, tests/conformance/test_loopback_bind.py.
      - name: loopback bind invariant
        run: |
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_bind.c \
            -o test-bind
          ./test-bind
      - name: vram registry tests
        # on macos-latest this is the only place plat_pid_start_time's sysctl
        # path executes on real Apple hardware (PID-reuse guard)
        run: |
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src \
            tests/test_vram_registry.c src/vramreg.c src/compat.c \
            -o test-vram-registry -lm -lpthread
          ./test-vram-registry
      - name: make test model
        run: python3 scripts/make-test-model.py test.gguf
      - name: Metal fallback ownership
        if: runner.os == 'macOS'
        run: make test-metal-fallback
      - name: focused json/schema tests
        run: |
          cc -std=gnu11 -Wall -Wextra -I src tests/test_json_schema.c \
            src/json.c src/jsonmode.c src/schema.c -o test-json-schema -lm
          ./test-json-schema
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_json_oom.c \
            -o test-json-oom -lm
          ./test-json-oom
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_schema_oom.c \
            src/jsonmode.c -o test-schema-oom -lm
          ./test-schema-oom
      - name: sampler preset tests
        run: |
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_sampler.c \
            src/sample.c -o test-sampler -lm
          ./test-sampler
      # runs against the committed vocabulary fixtures in tests/fixtures
      - name: tokenizer tests
        run: |
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tokenizer.c \
            src/gguf.c src/tokenizer.c src/compat.c src/quants.c -o test-tokenizer -lm
          ./test-tokenizer
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tokenizer_oom.c \
            src/gguf.c src/compat.c src/quants.c -o test-tokenizer-oom -lm
          ./test-tokenizer-oom
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_template.c \
            src/gguf.c src/tokenizer.c src/template.c src/json.c src/compat.c src/quants.c \
            -o test-template -lm
          ./test-template
          cc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tools.c \
            src/gguf.c src/tokenizer.c src/template.c src/schema.c src/jsonmode.c \
            src/json.c src/compat.c src/quants.c -o test-tools -lm
          ./test-tools
      - name: python client tests
        # PEP 668: the macOS runner's Homebrew python is externally managed and
        # refuses a bare pip install, so install into a venv instead
        run: |
          python3 -m venv .venv
          .venv/bin/python -m pip install --quiet pytest mypy
          MYPYPATH=python/src .venv/bin/python -m mypy --strict -p xyntetik_runner
          PYTHONPATH=python/src .venv/bin/python -m pytest python/tests/
      - name: sparse MoE synthetic correctness
        run: make test-moe PYTHON=.venv/bin/python
      - name: comparison and release-gate fixtures
        run: .venv/bin/python -m pytest tests/test_compare_llamacpp.py tests/test_release_check.py
      - name: torture harness unit tests
        # The scoring + cross-runtime comparison math the published matrix rests
        # on, gated offline: these exercise the harness itself (case generation,
        # verdict scoring, report diffing) with no live model. The self-conformance
        # run against Runner is a separate step below.
        run: .venv/bin/python -m pytest tests/test_agent_torture.py tests/test_torture_compare.py tests/test_competitor_freshness.py tests/test_write_stall_experiment.py
      - name: smoke test (completion)
        run: ./runner -m test.gguf -p "hello" -n 8 --temp 0
      - name: reject malformed tensor metadata
        run: |
          python3 scripts/make-test-model.py bad-zero.gguf --zero-first-dim
          python3 scripts/make-test-model.py bad-offset.gguf --wrap-first-offset
          for model in bad-zero.gguf bad-offset.gguf; do
            set +e
            ./runner -m "$model" -p hello -n 1 > /dev/null 2> "$model.err"
            status=$?
            set -e
            [ "$status" -eq 1 ]
            grep -q 'invalid tensor metadata' "$model.err"
          done
      - name: smoke test (bench json)
        run: |
          ./runner -m test.gguf --bench-json -n 8 --temp 0 2>/dev/null \
            | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['generated_tokens'] <= 8 and d['gen_tok_s'] >= 0; print('bench json ok')"
      - name: smoke test (suppress tokens)
        # all ids but EOS suppressed: generation must stop before emitting
        # anything, so stdout is exactly the echoed prompt
        run: |
          python3 scripts/make-test-model.py test-sup.gguf --suppress-all-but-eos
          OUT=$(./runner -m test-sup.gguf -p "hello" -n 8 --temp 0 2>/dev/null)
          [ "$OUT" = "hello" ] && echo "suppress ok"
      - name: smoke test (speculative decoding)
        # self-drafting at temp 0 must reproduce the plain greedy output exactly
        run: |
          A=$(./runner -m test.gguf -p "hello" -n 12 --temp 0 --gpu off 2>/dev/null)
          B=$(./runner -m test.gguf --draft test.gguf -p "hello" -n 12 --temp 0 --gpu off 2>/dev/null)
          [ "$A" = "$B" ] && [ -n "$A" ] && echo "speculative ok"
          ./runner -m test.gguf --draft test.gguf -p "hi" -n 24 --temp 0 --json --gpu off > spec-json.out 2> spec-json.err
          python3 -c "import json; json.load(open('spec-json.out')); print('spec json valid')"
          grep -q 'spec:' spec-json.err
      - name: smoke test (json mode)
        run: ./runner -m test.gguf -p "hi" -n 24 --temp 0 --json 2>/dev/null | python3 -c "import json,sys; json.load(sys.stdin); print('valid json')"
      - name: smoke test (server)
        run: |
          ./runner -m test.gguf --serve --port 8123 --parallel 2 &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8123/health && break; sleep 0.5; done
          curl -sf http://127.0.0.1:8123/health
          curl -sf http://127.0.0.1:8123/v1/models
          curl -sf http://127.0.0.1:8123/v1/capabilities | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['features']['json_schema'] and d['models'][0]['id']; print('capabilities ok')"
          # the family preset the server picked must be visible, and a client
          # must be able to turn the repeat penalty off per request
          curl -sf http://127.0.0.1:8123/v1/capabilities | python3 -c "import json,sys; d=json.load(sys.stdin); s=d['sampling']; assert s['preset'] and 'repeat_penalty' in s and d['features']['repeat_penalty']; print('sampling capabilities ok')"
          curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0,"repeat_penalty":1.0}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; json.load(sys.stdin)['choices'][0]['message']; print('repeat_penalty accepted')"
          [ "$(curl -s -o /dev/null -w '%{http_code}' -d '{"prompt":"x","repeat_penalty":0}' http://127.0.0.1:8123/v1/completions)" = 400 ]
          curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; d=json.load(sys.stdin); assert 'runner_telemetry' in d and 'prompt_cached_tokens' in d['runner_telemetry']; print('server valid json')"
          # prefix-cache regression: an identical greedy request must reuse the
          # cached KV and still return the identical completion
          R1=$(curl -sf -d '{"messages":[{"role":"user","content":"again"}],"max_tokens":8,"temperature":0}' http://127.0.0.1:8123/v1/chat/completions)
          R2=$(curl -sf -d '{"messages":[{"role":"user","content":"again"}],"max_tokens":8,"temperature":0}' http://127.0.0.1:8123/v1/chat/completions)
          C1=$(echo "$R1" | python3 -c "import json,sys; print(json.load(sys.stdin)['choices'][0]['message']['content'])")
          C2=$(echo "$R2" | python3 -c "import json,sys; print(json.load(sys.stdin)['choices'][0]['message']['content'])")
          [ "$C1" = "$C2" ] && echo "prefix cache ok"
          curl -sf -d '{"messages":[{"role":"user","content":"again"}],"max_tokens":2,"temperature":0,"cache_prompt":false}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['runner_telemetry']['prompt_cached_tokens'] == 0; print('cache bypass ok')"
          curl -sf -d '{"input":["a","b"]}' http://127.0.0.1:8123/v1/embeddings \
            | python3 -c "import json,sys; d=json.load(sys.stdin); assert len(d['data'])==2 and len(d['data'][0]['embedding'])==64; print('embeddings ok')"
          curl -sf -d '{"messages":[{"role":"user","content":"logprobs"}],"max_tokens":999999,"temperature":0,"logprobs":true,"top_logprobs":2}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['usage']['completion_tokens'] < 256; print('max_tokens clamp ok')"
          [ "$(curl -s -o /dev/null -w '%{http_code}' -d '{"prompt":"x","top_k":1e300}' http://127.0.0.1:8123/v1/completions)" = 400 ]
          [ "$(curl -s -o /dev/null -w '%{http_code}' -d '{"prompt":"x","keep_alive":1e300}' http://127.0.0.1:8123/v1/completions)" = 400 ]
          kill %1
      - name: smoke test (health bypasses a busy slot)
        # a stalled request pins the only slot; /health and /v1/models must
        # still answer from the accept loop (xyntetik watchdog regression)
        run: |
          ./runner -m test.gguf --serve --port 8126 --parallel 1 &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8126/health && break; sleep 0.5; done
          python3 -c "import socket,time; s=socket.create_connection(('127.0.0.1',8126)); s.sendall(b'POST /v1/chat/completions HTTP/1.1\r\n'); time.sleep(20)" &
          sleep 1
          curl -sf -m 3 http://127.0.0.1:8126/health
          curl -sf -m 3 http://127.0.0.1:8126/v1/models
          curl -sf -m 3 http://127.0.0.1:8126/v1/capabilities | python3 -c "import json,sys; assert json.load(sys.stdin)['features']['json_schema']; print('capabilities bypass ok')"
          kill %1 %2 2>/dev/null || true
          echo "health bypass ok"
      - name: smoke test (stalled request is reaped)
        # a client that never finishes its request must get a 408 and release
        # the slot within the 10s read deadline instead of pinning it forever
        run: |
          ./runner -m test.gguf --serve --port 8129 --parallel 1 &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8129/health && break; sleep 0.5; done
          python3 - <<'PYEOF'
          import socket, sys
          s = socket.create_connection(("127.0.0.1", 8129))
          s.sendall(b"POST /v1/chat/completions HTTP/1.1\r\nContent-Length: 100\r\n")
          s.settimeout(15)
          data = b""
          try:
              while True:
                  chunk = s.recv(4096)
                  if not chunk: break
                  data += chunk
          except socket.timeout:
              sys.exit("stalled connection not reaped within 15s")
          assert b"408" in data.split(b"\r\n", 1)[0], data[:60]
          PYEOF
          curl -sf -m 10 -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0}' \
            http://127.0.0.1:8129/v1/chat/completions > /dev/null
          kill %1
          echo "stall reap ok"
      - name: smoke test (stop sequences)
        # OpenAI "stop": truncates before the match (also mid-token-stream),
        # a partial match that never completes must flush, malformed -> 400
        run: |
          ./runner -m test.gguf --serve --port 8130 --parallel 1 &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8130/health && break; sleep 0.5; done
          python3 - <<'PYEOF'
          import json, urllib.request, urllib.error, sys
          def post(body, path="/v1/chat/completions"):
              req = urllib.request.Request(f"http://127.0.0.1:8130{path}",
                                           data=json.dumps(body).encode(),
                                           headers={"Content-Type": "application/json"})
              return json.load(urllib.request.urlopen(req, timeout=60))
          base = {"messages": [{"role": "user", "content": "hello"}],
                  "max_tokens": 24, "temperature": 0}
          text = post(dict(base))["choices"][0]["message"]["content"]
          clean = text.split("�")[0]  # invalid UTF-8 from the stub model
          assert len(clean) >= 3, repr(text)
          stop = clean[:3]
          expected = text[:text.find(stop)]
          r = post(dict(base, stop=[stop]))
          assert r["choices"][0]["message"]["content"] == expected
          assert r["choices"][0]["finish_reason"] == "stop"
          r = post(dict(base, stop=stop))
          assert r["choices"][0]["message"]["content"] == expected
          r = post(dict(base, stop=[clean + "NEVER"]))
          assert r["choices"][0]["message"]["content"] == text
          assert r["choices"][0]["finish_reason"] != "stop"
          req = urllib.request.Request("http://127.0.0.1:8130/v1/chat/completions",
                                       data=json.dumps(dict(base, stop=[stop], stream=True)).encode(),
                                       headers={"Content-Type": "application/json"})
          got, fin = [], None
          with urllib.request.urlopen(req, timeout=60) as resp:
              for line in resp:
                  line = line.decode().strip()
                  if not line.startswith("data: ") or line == "data: [DONE]":
                      continue
                  d = json.loads(line[6:])
                  got.append(d["choices"][0].get("delta", {}).get("content") or "")
                  fin = d["choices"][0].get("finish_reason") or fin
          assert "".join(got) == expected, "".join(got)
          assert fin == "stop", fin
          for bad in ([1], [""], ["a", "b", "c", "d", "e"], ""):
              try:
                  post(dict(base, stop=bad))
                  sys.exit(f"malformed stop {bad!r} accepted")
              except urllib.error.HTTPError as e:
                  assert e.code == 400
          caps = json.load(urllib.request.urlopen("http://127.0.0.1:8130/v1/capabilities", timeout=10))
          assert caps["features"]["stop_sequences"] is True
          PYEOF
          kill %1
          echo "stop sequences ok"
      - name: smoke test (cline-shaped traffic)
        # the @ai-sdk/openai-compatible wire format Cline/OpenCode send:
        # multipart content arrays, assistant tool_calls in history,
        # role:"tool" results, stream_options usage, extra fields
        run: |
          ./runner -m test.gguf -c 1024 --serve --port 8131 --parallel 1 &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8131/health && break; sleep 0.5; done
          python3 - <<'PYEOF'
          import json, urllib.request
          URL = "http://127.0.0.1:8131/v1/chat/completions"
          def post(body):
              req = urllib.request.Request(URL, data=json.dumps(body).encode(),
                                           headers={"Content-Type": "application/json"})
              return json.load(urllib.request.urlopen(req, timeout=60))
          def content(r): return r["choices"][0]["message"]["content"]
          flat = post({"messages": [{"role": "user", "content": "hel\nlo"}],
                       "max_tokens": 12, "temperature": 0})
          multi = post({"messages": [{"role": "user", "content": [
                            {"type": "text", "text": "hel"},
                            {"type": "text", "text": "lo"}]}],
                        "max_tokens": 12, "temperature": 0})
          assert content(multi) == content(flat), (content(multi), content(flat))
          call = {"id": "c1", "type": "function",
                  "function": {"name": "read_file", "arguments": "{\"path\":\"a.c\"}"}}
          looped = post({"messages": [
                             {"role": "user", "content": "go"},
                             {"role": "assistant", "content": None, "tool_calls": [call]},
                             {"role": "tool", "tool_call_id": "c1", "content": "data"}],
                         "max_tokens": 12, "temperature": 0})
          explicit = post({"messages": [
                               {"role": "user", "content": "go"},
                               {"role": "assistant",
                                "content": "<|tool_call>call:read_file{\"path\":\"a.c\"}<tool_call|>"},
                               {"role": "tool", "tool_call_id": "c1", "content": "data"}],
                           "max_tokens": 12, "temperature": 0})
          assert content(looped) == content(explicit), (content(looped), content(explicit))
          body = json.dumps({"messages": [{"role": "user", "content": "hi"}],
                             "max_tokens": 8, "temperature": 0, "stream": True,
                             "stream_options": {"include_usage": True}}).encode()
          req = urllib.request.Request(URL, data=body,
                                       headers={"Content-Type": "application/json"})
          usage, done = None, False
          with urllib.request.urlopen(req, timeout=60) as resp:
              for line in resp:
                  line = line.decode().strip()
                  if line == "data: [DONE]": done = True; continue
                  if not line.startswith("data: "): continue
                  d = json.loads(line[6:])
                  usage = d.get("usage") or usage
          assert done and usage and usage["prompt_tokens"] > 0, usage
          r = post({"messages": [{"role": "user", "content": [{"type": "text", "text": "x"}]}],
                    "max_tokens": 8, "temperature": 0,
                    "tools": [{"type": "function", "function":
                               {"name": "f", "description": "d",
                                "parameters": {"type": "object", "properties": {}}}}],
                    "tool_choice": "auto", "parallel_tool_calls": False,
                    "frequency_penalty": 0, "presence_penalty": 0, "user": "cline"})
          assert r["choices"][0]["finish_reason"] in ("stop", "length", "tool_calls")
          PYEOF
          kill %1
          echo "cline-shaped traffic ok"
      - name: smoke test (parent-pid watch)
        # the runner must exit when the process named by --parent-pid dies
        run: |
          python3 - <<'PYEOF'
          import os, subprocess, sys, time
          exe = "./runner.exe" if os.name == "nt" else "./runner"
          p = subprocess.Popen([exe, "-m", "test.gguf", "--serve", "--port", "8127",
                                "--parent-pid", str(os.getpid())])
          time.sleep(2)
          if p.poll() is not None: sys.exit("runner died early")
          os._exit(0)  # abrupt parent death, no cleanup
          PYEOF
          sleep 6
          if curl -s -m 2 http://127.0.0.1:8127/health > /dev/null; then
            echo "runner survived parent death"; exit 1
          fi
          echo "parent-pid ok"
      - name: smoke test (json schema)
        run: |
          echo '{"type":"object","properties":{"a":{"type":"string","enum":["x","y"]},"b":{"type":"integer"}},"required":["a","b"]}' > ts.json
          ./runner -m test.gguf --json-schema ts.json -p "hi" -n 30 --temp 0 2>/dev/null | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['a'] in ('x','y') and isinstance(d['b'], int); print('schema ok')"
          echo '{"type":"array","items":{"type":"string"},"minItems":2,"maxItems":1}' > bad-array.json
          if ./runner -m test.gguf --json-schema bad-array.json -p "hi" -n 4 --temp 0 >/dev/null 2>&1; then
            echo "bad array bounds unexpectedly accepted"; exit 1
          fi
          python3 - <<'PYEOF'
          import json
          with open("bad-key.json", "w", encoding="utf-8") as f:
              json.dump({"type":"object","properties":{'bad"key':{"type":"string"}}}, f)
          PYEOF
          if ./runner -m test.gguf --json-schema bad-key.json -p "hi" -n 4 --temp 0 >/dev/null 2>&1; then
            echo "escaped property key unexpectedly accepted"; exit 1
          fi
          echo "schema rejects ok"
      - name: smoke test (quantize)
        run: |
          ./runner -m test.gguf --quantize test-q8.gguf --quant q8_0
          ./runner -m test-q8.gguf -p "hi" -n 4 --temp 0
      - name: quantize reports write failures
        if: runner.os == 'Linux'
        run: |
          if ./runner -m test.gguf --quantize /dev/full --quant q8_0; then
            echo "quantize ignored /dev/full write failure"; exit 1
          fi
      - name: smoke test (model swap)
        run: |
          printf 'not a gguf' > bad.gguf
          ./runner -m "a=test.gguf,b=test-q8.gguf" --serve --port 8124 --ttl 0 &
          SWAP_PID=$!
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8124/health && break; sleep 0.5; done
          curl -sf -d '{"model":"a","messages":[{"role":"user","content":"x"}],"max_tokens":3}' http://127.0.0.1:8124/v1/chat/completions > /dev/null
          curl -sf -d '{"model":"b","messages":[{"role":"user","content":"x"}],"max_tokens":3}' http://127.0.0.1:8124/v1/chat/completions > /dev/null
          curl -sf http://127.0.0.1:8124/v1/models | python3 -c "import json,sys; assert len(json.load(sys.stdin)['data']) == 2; print('swap ok')"
          curl -sf http://127.0.0.1:8124/v1/capabilities | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['swap'] and len(d['models']) == 2; print('swap capabilities ok')"
          kill "$SWAP_PID" 2>/dev/null || true
          wait "$SWAP_PID" 2>/dev/null || true
          ./runner -m "good=test.gguf,bad=bad.gguf" --serve --port 8124 --ttl 0 &
          BAD_SWAP_PID=$!
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8124/health && break; sleep 0.5; done
          if curl -sf -d '{"model":"bad","messages":[{"role":"user","content":"x"}],"max_tokens":3}' http://127.0.0.1:8124/v1/chat/completions > /dev/null; then
            echo "bad swap unexpectedly loaded"; exit 1
          fi
          curl -sf -d '{"model":"good","messages":[{"role":"user","content":"x"}],"max_tokens":3}' http://127.0.0.1:8124/v1/chat/completions > /dev/null
          kill "$BAD_SWAP_PID" 2>/dev/null || true
          wait "$BAD_SWAP_PID" 2>/dev/null || true
          echo "bad swap cleanup ok"
      - name: smoke test (server speculative decoding)
        # a self-draft server at temp 0 must match the plain server's output
        # byte-for-byte, and a json-constrained request must still succeed
        # through the constrained speculative path.
        run: |
          REQ='{"messages":[{"role":"user","content":"hi"}],"max_tokens":12,"temperature":0}'
          ./runner -m test.gguf --serve --port 8128 --gpu off &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8128/health && break; sleep 0.5; done
          P=$(curl -sf -d "$REQ" http://127.0.0.1:8128/v1/chat/completions | python3 -c "import json,sys; print(json.load(sys.stdin)['choices'][0]['message']['content'])")
          kill %1; wait %1 2>/dev/null || true
          ./runner -m test.gguf --draft test.gguf --serve --port 8128 --gpu off &
          # readiness poll, not a fixed sleep: the paravirtual macOS
          # runner lost a 2s race by 250ms twice on 2026-08-12
          for i in $(seq 60); do curl -sf -o /dev/null http://127.0.0.1:8128/health && break; sleep 0.5; done
          S=$(curl -sf -d "$REQ" http://127.0.0.1:8128/v1/chat/completions | python3 -c "import json,sys; print(json.load(sys.stdin)['choices'][0]['message']['content'])")
          [ -n "$P" ] || exit 1
          [ "$P" = "$S" ] || { echo "spec output diverged: '$P' vs '$S'"; exit 1; }
          # capture the response first: a heredoc on the same pipeline would
          # replace curl's output as python3's stdin (python3 would read the
          # heredoc as program text instead), so json.load(sys.stdin) would
          # always hit EOF and fail this step on every run
          RESP=$(curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":24,"temperature":0,"response_format":{"type":"json_object"}}' \
            http://127.0.0.1:8128/v1/chat/completions)
          printf '%s' "$RESP" | python3 -c '
          import json, sys
          d = json.load(sys.stdin)
          content = d["choices"][0]["message"]["content"]
          finish_reason = d["choices"][0].get("finish_reason", "")
          # near-random stub model can hit length before emitting "{" depending on host FP codegen
          if finish_reason == "length" and not content.startswith("{"):
              print("spec json: skipped (incomplete due to context length)")
              sys.exit(0)
          json.loads(content)
          '
          kill %1
          echo "server spec ok"
      - name: sanitizer build + run
        # The GPU smoke keeps leak detection off because the loaded driver owns
        # process-lifetime allocations. The Linux CPU smoke has no such noise
        # and is the leak gate for CLI-owned model/tokenizer/engine state.
        env:
          ASAN_OPTIONS: detect_leaks=0
        run: |
          make debug
          ./runner-debug -m test.gguf -p "hello" -n 8 --temp 0
          if [ "$(uname -s)" = Linux ]; then
            ASAN_OPTIONS=detect_leaks=1 ./runner-debug -m test.gguf \
              -p "hello" -n 8 --temp 0 --gpu off
          fi
      - name: sanitized server race gate
        # The CLI run above never starts a server, so nothing sanitized ever
        # exercised the accept thread against a slot thread. This one does:
        # /v1/capabilities is answered on the accept loop while another client
        # swaps the model out from under it.
        run: make test-swap-race

  # The `make test` baseline. Deliberately boring: one Linux CPU runner, one
  # build, one target — so that ADDING A CHECK TO `make test` IS ENOUGH TO PUT
  # IT IN CI. Every gate above is a hand-written step, which means a check can
  # be added to the local target and never run here; that is how the generated
  # GPU header gate sat un-CI'd until 2026-08-18, and it is a structural gap,
  # not an oversight anyone can be careful about.
  #
  # It overlaps the `unix` job on purpose and nothing is deduped: `unix` covers
  # macOS as well and runs steps `make test` does not (the sanitizer build, the
  # server smokes), while `make test` runs gates `unix` never names. Two lists
  # that each catch what the other misses beats one list that has to be kept
  # complete by hand.
  #
  # CPU-only by construction: the runner image has no GPU, so the Metal targets
  # self-skip as macOS-only and the CUDA-linked tolerance gates report
  # "skipped: no GPU" instead of failing. Nothing here downloads a model —
  # `make test` builds its own fixtures with scripts/make-test-model.py.
  make-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      # PEP 668: keep the venv convention the `unix` job already uses, and pass
      # it in as PYTHON= so every recipe in the target — fixture generation as
      # well as pytest — runs against the interpreter that has pytest. Without
      # it `make test` prints "Python client tests skipped" and passes with the
      # whole pytest half unexecuted.
      - name: install pytest
        run: |
          python3 -m venv .venv
          .venv/bin/python -m pip install --quiet pytest
      # Separate from `make test` (which would build it as a prerequisite) so a
      # compile failure is legible as one, matching the other jobs.
      - name: build
        run: make
      - name: make test
        run: make test PYTHON=.venv/bin/python

  # libFuzzer harnesses for the hand-written parsers that eat untrusted input.
  # Bounded by design: this is a regression gate that has to finish in a couple
  # of minutes, not a fuzzing campaign. Long soaks belong on a schedule.
  fuzz:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: install clang
        run: sudo apt-get update && sudo apt-get install -y clang llvm
      # the harnesses must keep compiling even when nobody runs them locally
      - name: build harnesses
        run: make fuzz-build FUZZ_CLANG=clang
      - name: fuzz (bounded)
        run: make fuzz FUZZ_CLANG=clang FUZZ_TIME=30
      # a crash artifact is the whole point of the job: keep it so the failure
      # can be reproduced locally with ./fuzz-<target> <artifact>
      - name: upload crash artifacts
        if: failure()
        uses: actions/upload-artifact@v7
        with:
          name: fuzz-crashes
          path: fuzz-corpus/*/crash-*
          if-no-files-found: ignore
      # `make fuzz` must be a no-op, not an error, where clang is absent
      - name: no-op without clang
        run: |
          OUT=$(make fuzz FUZZ_CLANG=definitely-not-a-compiler)
          echo "$OUT"
          echo "$OUT" | grep -q 'skipped'

  windows:
    runs-on: windows-latest
    defaults:
      run:
        shell: msys2 {0}
    steps:
      - uses: actions/checkout@v7
      - uses: msys2/setup-msys2@v2
        with:
          msystem: UCRT64
          update: false
          install: >-
            make
            mingw-w64-ucrt-x86_64-gcc
            mingw-w64-ucrt-x86_64-python
            mingw-w64-ucrt-x86_64-python-pytest
      - name: build
        run: make
      - name: reject invalid CLI values
        run: |
          for args in '--port 70000' '--gpu typo' '--kv typo' '--reserve 101'; do
            if ./runner.exe --caps $args > /dev/null 2>&1; then
              echo "unexpectedly accepted $args"; exit 1
            fi
          done
      # see the unix job: the loopback bind is enforced, not assumed
      - name: loopback bind invariant
        run: |
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_bind.c \
            -o test-bind.exe
          ./test-bind.exe
      - name: make test model
        run: python3 scripts/make-test-model.py test.gguf
      - name: focused json/schema tests
        run: |
          gcc -std=gnu11 -Wall -Wextra -I src tests/test_json_schema.c \
            src/json.c src/jsonmode.c src/schema.c -o test-json-schema.exe -lm
          ./test-json-schema.exe
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_json_oom.c \
            -o test-json-oom.exe -lm
          ./test-json-oom.exe
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_schema_oom.c \
            src/jsonmode.c -o test-schema-oom.exe -lm
          ./test-schema-oom.exe
      - name: sampler preset tests
        run: |
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_sampler.c \
            src/sample.c -o test-sampler.exe -lm
          ./test-sampler.exe
      # runs against the committed vocabulary fixtures in tests/fixtures
      - name: tokenizer tests
        run: |
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tokenizer.c \
            src/gguf.c src/tokenizer.c src/compat.c src/quants.c -o test-tokenizer.exe -lm
          ./test-tokenizer.exe
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tokenizer_oom.c \
            src/gguf.c src/compat.c src/quants.c -o test-tokenizer-oom.exe -lm
          ./test-tokenizer-oom.exe
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_template.c \
            src/gguf.c src/tokenizer.c src/template.c src/json.c src/compat.c src/quants.c \
            -o test-template.exe -lm
          ./test-template.exe
          gcc -std=gnu11 -Wall -Wextra -Wno-unused-parameter -I src tests/test_tools.c \
            src/gguf.c src/tokenizer.c src/template.c src/schema.c src/jsonmode.c \
            src/json.c src/compat.c src/quants.c -o test-tools.exe -lm
          ./test-tools.exe
      - name: python client tests
        run: PYTHONPATH=python/src python3 -m pytest python/tests/
      - name: smoke test (completion)
        run: ./runner.exe -m test.gguf -p "hello" -n 8 --temp 0
      - name: reject malformed tensor metadata
        run: |
          python3 scripts/make-test-model.py bad-zero.gguf --zero-first-dim
          python3 scripts/make-test-model.py bad-offset.gguf --wrap-first-offset
          for model in bad-zero.gguf bad-offset.gguf; do
            set +e
            ./runner.exe -m "$model" -p hello -n 1 > /dev/null 2> "$model.err"
            status=$?
            set -e
            [ "$status" -eq 1 ]
            grep -q 'invalid tensor metadata' "$model.err"
          done
      - name: smoke test (bench json)
        run: |
          ./runner.exe -m test.gguf --bench-json -n 8 --temp 0 2>/dev/null \
            | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['generated_tokens'] <= 8 and d['gen_tok_s'] >= 0; print('bench json ok')"
      - name: smoke test (suppress tokens)
        run: |
          python3 scripts/make-test-model.py test-sup.gguf --suppress-all-but-eos
          OUT=$(./runner.exe -m test-sup.gguf -p "hello" -n 8 --temp 0 2>/dev/null)
          [ "$OUT" = "hello" ] && echo "suppress ok"
      - name: smoke test (json mode)
        run: ./runner.exe -m test.gguf -p "hi" -n 24 --temp 0 --json 2>/dev/null | python3 -c "import json,sys; json.load(sys.stdin); print('valid json')"
      - name: smoke test (quantize)
        run: |
          ./runner.exe -m test.gguf --quantize test-q8.gguf --quant q8_0
          ./runner.exe -m test-q8.gguf -p "hi" -n 4 --temp 0
      - name: smoke test (server)
        run: |
          ./runner.exe -m test.gguf --serve --port 8123 --parallel 2 &
          sleep 3
          curl -sf http://127.0.0.1:8123/health
          curl -sf http://127.0.0.1:8123/v1/capabilities | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['features']['json_schema']; print('capabilities ok')"
          # the family preset the server picked must be visible, and a client
          # must be able to turn the repeat penalty off per request
          curl -sf http://127.0.0.1:8123/v1/capabilities | python3 -c "import json,sys; d=json.load(sys.stdin); s=d['sampling']; assert s['preset'] and 'repeat_penalty' in s and d['features']['repeat_penalty']; print('sampling capabilities ok')"
          curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0,"repeat_penalty":1.0}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; json.load(sys.stdin)['choices'][0]['message']; print('repeat_penalty accepted')"
          [ "$(curl -s -o /dev/null -w '%{http_code}' -d '{"prompt":"x","repeat_penalty":0}' http://127.0.0.1:8123/v1/completions)" = 400 ]
          curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0}' \
            http://127.0.0.1:8123/v1/chat/completions | python3 -c "import json,sys; d=json.load(sys.stdin); assert 'runner_telemetry' in d; print('server valid json')"
          [ "$(curl -s -o /dev/null -w '%{http_code}' -d '{"prompt":"x","top_k":1e300}' http://127.0.0.1:8123/v1/completions)" = 400 ]
          kill %1
      - name: smoke test (server, backslash model path)
        # regression: a C:\...\model.gguf -m path used to land unescaped in the
        # response's "model" field, making every completion body invalid JSON
        run: |
          ./runner.exe -m "$(cygpath -w "$PWD")\\test.gguf" --serve --port 8125 &
          sleep 3
          curl -sf -d '{"messages":[{"role":"user","content":"hi"}],"max_tokens":4,"temperature":0}' \
            http://127.0.0.1:8125/v1/chat/completions | python3 -c "import json,sys; d=json.load(sys.stdin); assert d['model'] == 'test.gguf', d['model']; print('windows path model field ok')"
          kill %1
      - name: smoke test (parent-pid watch)
        # the runner must exit when the process named by --parent-pid dies
        run: |
          python3 - <<'PYEOF'
          import os, subprocess, sys, time
          exe = "./runner.exe" if os.name == "nt" else "./runner"
          p = subprocess.Popen([exe, "-m", "test.gguf", "--serve", "--port", "8127",
                                "--parent-pid", str(os.getpid())])
          time.sleep(2)
          if p.poll() is not None: sys.exit("runner died early")
          os._exit(0)  # abrupt parent death, no cleanup
          PYEOF
          sleep 6
          if curl -s -m 2 http://127.0.0.1:8127/health > /dev/null; then
            echo "runner survived parent death"; exit 1
          fi
          echo "parent-pid ok"

  # Phase 0 agent-protocol conformance harness. Runs against the synthetic
  # test.gguf so it needs no model download and no GPU. Locally: scripts/conformance.sh
  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: build
        run: make
      - name: make test model
        run: python3 scripts/make-test-model.py test.gguf
      - name: install pytest
        run: |
          python3 -m venv .venv
          .venv/bin/python -m pip install --quiet pytest
      # The Anthropic SDK turns the /v1/messages tests in test_messages.py from
      # a hand-written shape check into a real client parsing real events —
      # which is the check that found bugs a spec reading did not. It is not
      # required: those tests skip when it is absent, so a registry outage
      # degrades the run rather than failing it.
      - name: install the Anthropic SDK (optional, for real-client checks)
        continue-on-error: true
        run: .venv/bin/python -m pip install --quiet anthropic
      - name: agent-protocol conformance harness
        run: PYTHON=.venv/bin/python ./scripts/conformance.sh
      - name: conformance report
        if: always()
        run: |
          test -f tests/conformance/out/report.json
          python3 -c "
          import json
          d = json.load(open('tests/conformance/out/report.json'))
          print(json.dumps(d['totals'], indent=2))
          print(json.dumps(d['latency_ms'], indent=2))
          print('peak_rss_kb', d['resources'])
          for n in d['model_quality_notes']: print('quality:', n['test'], '-', n['message'])
          "
      # The agent torture matrix against Runner itself: schema enforcement makes
      # even the tiny test fixture pass every adversarial case, so ANY failure
      # is a Runner conformance regression, not a weak model. Standing guard on
      # the exact claim the cross-runtime leaderboard makes.
      - name: agent torture (self-conformance)
        run: |
          python3 scripts/agent-torture.py --cases 40 --out tests/torture/out
          python3 -c "
          import json, sys
          d = json.load(open('tests/torture/out/report.json'))
          t = d['totals']
          print('agent-torture', json.dumps(t))
          sys.exit(1 if t['failed'] else 0)
          "
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: conformance-report
          path: |
            tests/conformance/out/
            tests/torture/out/

  # Real SDK/gateway/framework parsers catch integration assumptions that raw
  # JSON fixtures cannot. Dependencies are pinned and isolated from Runner.
  consumer-compatibility:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: tests/compatibility/node/package-lock.json
      - name: build and make test model
        run: |
          make
          python3 scripts/make-test-model.py test.gguf
      - name: install pinned consumers
        run: |
          python3 -m venv .compat-venv
          .compat-venv/bin/pip install --quiet -r tests/compatibility/requirements.txt
          npm ci --ignore-scripts --prefix tests/compatibility/node
      - name: official SDK, LiteLLM and LangChain compatibility
        run: .compat-venv/bin/python scripts/consumer_compat.py
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: consumer-compatibility-report
          path: tests/compatibility/out/consumers.json

  # Chat-template conformance: runner renders chat templates natively in C, so
  # the ONLY thing standing between a family and silent drift is a comparison
  # against the model's own jinja template. The `unix` job above builds and
  # runs tests/test_template.c, but that gate cannot find drift: its goldens
  # were written from runner's own output, so it proves self-consistency and
  # nothing else. That is precisely how 30 real differences accumulated across
  # 8 families unnoticed. This job is the oracle.
  #
  # It gets its own job rather than a step in `unix`, for two reasons:
  #   - it is the one gate here that talks to a third party (huggingface.co).
  #     Isolated, a red here is unambiguous and does not tar the build matrix.
  #   - it reports three outcomes, not two: exit 2 means NOT CHECKED (oracle
  #     unreachable), never "conformant". Read the summary line before
  #     assuming a red is drift. The fetch already retries 3x on transport
  #     errors, so a red is more likely real than not.
  #
  # --network-only restricts it to the families whose reference is fetched
  # over HTTP. gemma/gemma4/granite/harmony read their template out of a
  # multi-GB local GGUF and are simply not checkable on a CI runner; asking
  # for them would guarantee a permanent exit 2. Those four are covered by
  # `make template-conformance` on a dev box with models/, and their backlog
  # stays recorded in scripts/template-conformance-baseline.json either way.
  template-conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - name: install jinja2
        # jinja2 renders the REFERENCE side. Without it nothing is compared,
        # and the harness says so (exit 2) instead of passing.
        run: |
          python3 -m venv .tmpl-venv
          .tmpl-venv/bin/python -m pip install --quiet jinja2
      - name: runner C renderer vs upstream jinja templates
        run: .tmpl-venv/bin/python scripts/template-conformance.py --network-only
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: template-oracles
          path: .build/template-oracles/
